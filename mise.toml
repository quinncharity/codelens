[tools]
# Required by DSPy RLM sandbox (Pyodide/WASM)
deno = "2"

# Used by the monorepo (pnpm/turbo + TypeScript tooling)
node = "22"
pnpm = "10.28.0"

# Used by services/analyzer (see services/analyzer/package.json scripts)
python = "3.13"
uv = "latest"

