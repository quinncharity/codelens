# CodeLens web app

TanStack Start + React UI for CodeLens. See the [repository README](../../README.md) for setup, environment variables, and how to run the full stack.

```bash
# From repo root (recommended)
pnpm dev

# This package only
pnpm --filter @codelens/web dev
```
